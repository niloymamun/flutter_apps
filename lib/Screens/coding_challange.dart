import 'package:flutter/material.dart';

class CodingChallengesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("কোডিং চ্যালেঞ্জ"),
      ),
      body: Center(
        child: Text("কোডিং চ্যালেঞ্জ পৃষ্ঠা"),
      ),
    );
  }
}
