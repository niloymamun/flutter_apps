import 'package:flutter/material.dart';

class QuizPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("কুইজ"),
      ),
      body: Center(
        child: Text("কুইজ পৃষ্ঠা"),
      ),
    );
  }
}
