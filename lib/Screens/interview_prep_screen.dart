import 'package:flutter/material.dart';

class InterviewPreparationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("ইন্টারভিউ প্রস্তুতি"),
      ),
      body: Center(
        child: Text("ইন্টারভিউ প্রস্তুতি পৃষ্ঠা"),
      ),
    );
  }
}
